---
name: mutter-workers
description: Mutter workspace command — workers (see skill `workers`).
---

# mutter workers

Run the **workers** workflow from the Mutter plugin.

1. Open `.mutter/core/project.md` for orientation.
2. Invoke the matching skill **workers** so full instructions load.
3. For epics: keep state in `.mutter/plans/`, `.mutter/tasks/`, and `.mutter/state/current.json`; use read-only parallel exploration only on disjoint paths.

For slash-style usage in Claude Code, use `/mutter:workers`. In Cursor, use the Command palette entry or ask the agent to follow the **workers** skill.
