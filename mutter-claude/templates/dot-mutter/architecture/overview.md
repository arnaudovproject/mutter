# Architecture overview

Maintain a **short** high-level map: major components, boundaries, and how requests flow.

Update when structure changes. Link to detail files (`backend.md`, `frontend.md`, etc.) instead of duplicating long prose here.
