# Index shards

Place **small JSON** index files here, for example:

- `backend.index.json`
- `frontend.index.json`
- `api.index.json`
- `dependencies.index.json`

Each entry should point to a **path**, **domain**, **related** symbols, and **tests**—never full file contents.
