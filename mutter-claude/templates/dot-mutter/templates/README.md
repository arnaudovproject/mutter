# Templates

Task templates, plan skeletons, ADR templates for copy into `tasks/current/` or `plans/`.

| File | Use |
|------|-----|
| `TASK.md` | Copy to `tasks/current/<slug>.md` when creating work; fill **Acceptance** and **Verify** before deep execution. |
