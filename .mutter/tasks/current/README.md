# Current tasks

One file per active task (markdown). Include status, steps, affected domains, affected file paths, **acceptance** (checkable outcomes), and **verification** commands.

Skeleton: copy `../templates/TASK.md` into this folder and rename.
