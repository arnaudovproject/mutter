# Workflow: release

1. Verify roadmap item and version bump task.
2. Run full tests as per `.mutter/memory/` testing conventions.
3. Snapshot index/architecture optional under `snapshots/`.
4. Update `roadmap/` and archive completed tasks to `tasks/completed/`.
